Ne pas prêtez grande attention ce repo , ceci est un project nodejs réaliser par moi pour tester ma capacité a sécurise des secrets sur un backend distant !!

FAIT AVEC EXPRESS + CORS + DOTENV !

'render.com' est utilisé ici comme hébergeur (avec un temps de latence très long malheureusement, 40ms )

_________________________________________


Don't pay too much attention to this repository; this is a Node.js project I created to test my ability to secure secrets on a remote backend!

MADE WITH EXPRESS + CORS + DOTENV!

'render.com' is used here as the host (with unfortunately very high latency, 40ms).
